{
  "name": "firm-client",
  "version": "0.1.0",
  "description": "Web Serial + Rust WASM client reading data packets from FIRM",
  "license": "MIT",
  "type": "module",

  "main": "ts/dist/index.js",
  "module": "ts/dist/index.js",
  "types": "ts/dist/index.d.ts",

  "files": [
    "ts/dist",
    "ts/pkg/firm_client.js",
    "ts/pkg/firm_client_bg.wasm",
    "ts/pkg/firm_client.d.ts",
    "LICENSE",
    "README.md"
  ],

  "scripts": {
    "clean": "rimraf ts/pkg ts/dist",
    "build:wasm": "wasm-pack build --target web --out-dir ts/pkg --out-name firm_client --no-default-features --features wasm",
    "build:ts": "cd ts && tsc",
    "build": "npm run build:wasm && npm run build:ts",
    "prepare": "npm run build"
  },

  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.0.0"
  }
}
